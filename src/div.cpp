#include <iostream>

int divide(int numerador, int denominador) {
    int q, r;

    

    return q;
}

int main() {
    int a, b;
    int resultado;

    if (resultado = divide(a, b)) {
        std::cout << a << " / " << b << " = " << resultado << '\n';
    } else {
        std::cout << "Erro: divisão por zero\n";
    }

    return 0;
}